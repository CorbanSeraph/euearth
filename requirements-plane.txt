# plane/ — real frozen-base adapter + router plane (needs Python 3.11/3.12; torch has no 3.14 wheels yet)
cryptography>=42
torch>=2.3
transformers>=4.44,<5
peft>=0.11
safetensors>=0.4
numpy>=1.26
accelerate>=0.30
datasets>=2.19
